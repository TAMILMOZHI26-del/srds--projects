import React from 'react';
import './ImageContainer.css';  // Import the CSS file

const ImageContainer = () => {
  return (
    <div className="image-container"></div>  
  );
};

export default ImageContainer;
